const MessageSkeleton = () => {
  const skeletonMessages = Array(6).fill(null);
  return <>
  <div className='flex-1 overflow-y-auto p-4space-y-4'>
    
  </div>
  
  </>;
};

export default MessageSkeleton;